version: 2

models:
  - name: dim_movie_genre
    description: Dimension with unique genres from the IMDB movies data set
    columns:
      - name: genre
        data_tests:
          - not_null
          - unique
